1.模块级方法
    active_count()
    current_thread()
    get_ident()
    enumerate()
    main_thread()
2.线程私有存储
    local() # type = class

3.线程对象
   start()
   run()
   join():block calling thread until called thread terminite
   daemon: inherite from creating thread , default is False

4.线程同步
    Lock
    RLock: 递归锁
    Condition()
    Semaphore: 锁池
    Event
    Timer
    Barrier: 统一界限

threading.current_thread() # 获取当前线程
