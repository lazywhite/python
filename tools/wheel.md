## Introduction
Wheel is a built-package format and offer the advantage of not recompiling 
your software during every install.

wheel package is "zip-format" 

## Usage
```
pip install wheel
pip install /path/to/file.whl

unzip /path/to/file.whl
```
